/**
 * Игра встроена в тот же сайт, что и книга — клиенту достаточно одной ссылки на главную.
 *
 * - `panel` — «Крутим удачу?» открывает полную игру поверх интерфейса (рекомендуется для клиента).
 * - `route` — переход на отдельную страницу `/luck` (удобно для отладки).
 *
 * Маршрут `/luck` в приложении сохранён и работает при любом значении ниже.
 */
export type LuckyWheelEntryMode = "panel" | "route";

/**
 * `panel` — игра панелью поверх книги (один URL).
 * `route` — отдельная вкладка `/luck`.
 */
export const LUCKY_WHEEL_ENTRY: LuckyWheelEntryMode = "panel";

/** Сколько оборотов за одну сессию (ТЗ: 3–4). */
export const LUCKY_SPINS_PER_GAME = 4;
